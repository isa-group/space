apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-data
  name: redis-data
  namespace: space
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
